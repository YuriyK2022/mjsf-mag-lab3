export type Product = {
    id: number;
    name: string;
    price: number;
    category: string;
  };
  
  export type User = {
    id: number;
    username: string;
    email: string;
    role: string;
  };
  